header.header 
    .container-fluid 
        .row.align-items-center 
            .col-8
                .inner-logo 
                    a(href="/admin/dashboard") Trang quản trị
            .col-4
                .row
                    div(class="text-end" )
                        a( href=`${prefixAdmin}/my-account`
                        class="btn btn-primary btn-sm me-2"
                        ) #{user.fullName} 
                        a(
                            href=`${prefixAdmin}/auth/logout`
                            class="btn btn-danger btn-sm"
                        ) Đăng xuất
