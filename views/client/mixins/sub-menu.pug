mixin sub-menu(items,patch)
    ul 
        each item in items
            li 
                a(href=`${patch}/${item.slug}`)  #{item.title}
                if item.children && item.children.length > 0
                    +sub-menu(item.children,patch)