extends ../../layout/default
include ../../mixins/products.layout.pug
include ../../mixins/box-head.pug
block main 
    .container.my-3
        .row
            .col-12
                +box-head("Kết quả tìm kiếm")
        if (products.length > 0)
            +products-list(products)
        else 
            h1 Không tìm thấy sản phẩm này