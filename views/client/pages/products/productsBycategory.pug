extends ../../layout/default.pug

include ../../mixins/box-head.pug 
include ../../mixins/products.layout.pug
block main
    if (products.length > 0)
        .container.my-3 
            .row 
                .col-12 
                    +box-head("Danh sách sản phẩm")
            +products-grid(products)

    else 
        h1 Không có sản phẩm trong danh mục này