include ../mixins/sub-menu.pug
header.header 
    .container 
        .row.align-items-center
            .col-3 
                .inner-logo 
                    a(href="/")
                        img(src="/img/logo.png", alt="Logo")
            .col-9
                .inner-menu 
                    ul 
                        li 
                            a(href="/") Trang chủ
                        li(class="sub-menu")
                            -const patchProducts = "/products"
                            a(href=patchProducts) Sản phẩm
                                +sub-menu(LayoutProductsCategory,patchProducts)